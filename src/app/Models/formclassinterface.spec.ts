import { Formclassinterface } from './formclassinterface';

describe('Formclassinterface', () => {
  it('should create an instance', () => {
    expect(new Formclassinterface()).toBeTruthy();
  });
});
